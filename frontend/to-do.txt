Editá el tailwind.config.js:

js
Copy
Edit
content: [
  "./index.html",
  "./src/**/*.{js,ts,jsx,tsx}",
],
theme: {
  extend: {},
},
plugins: [],
En src/index.css:

css
Copy
Edit
@tailwind base;
@tailwind components;
@tailwind utilities;
✅ 5. Configurar ESLint + Prettier
🔹 En el frontend (React)
bash
Copy
Edit
npm install -D eslint prettier eslint-config-prettier eslint-plugin-react eslint-plugin-react-hooks
npx eslint --init
Elegí:

"To check syntax, find problems, and enforce code style"

"React"

"JavaScript modules (import/export)"

"Browser"

"Use a popular style guide" → Airbnb

"JavaScript"

"Yes" para instalar dependencias

Después instalá Prettier:

bash
Copy
Edit
npm install -D prettier eslint-config-prettier
Agregá esto a tu .eslintrc.cjs:

js
Copy
Edit
extends: [
  "airbnb",
  "airbnb/hooks",
  "plugin:react/recommended",
  "prettier"
],
Y en package.json:

json
Copy
Edit
"prettier": {
  "singleQuote": true,
  "semi": false
}
Podés crear un script:

json
Copy
Edit
"scripts": {
  "lint": "eslint . --ext .js,.jsx"
}